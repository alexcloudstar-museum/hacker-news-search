import styled from 'styled-components';
import { Box } from '@material-ui/core';

export const StyledPostWrapper = styled(Box)`
	h3 {
		margin-right: 5px;
	}
`;
export const PostHeading = styled(Box)`
	display: flex;
`;
